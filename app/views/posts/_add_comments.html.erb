<div>
<%= form_with url:"/users/#{@post.author_id}/posts/#{@post.id}/comments", scope: :comment do |f| %>
    <div>
     <%= f.text_area :text, rows: 10, required: true %>
     <%= f.submit "Comment" %>
     </div>
<%end%>
</div>